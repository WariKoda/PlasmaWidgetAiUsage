# Plasma 5.27 Port Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a dedicated Plasma 5.27 branch that preserves all provider behavior while replacing Plasma 6-specific QML and packaging interfaces.

**Architecture:** Keep the Python helper and normalized data contract shared. Restrict platform differences to versioned QML imports, the plasmoid root/attached-property interface, executable data sources, configuration root, metadata, installer, and documentation.

**Tech Stack:** Plasma 5.27, Qt 5.15 QML, KDE Frameworks 5, Python 3.10+ stdlib.

## Global Constraints

- Do not create a worktree.
- Do not modify provider behavior or output schemas.
- Do not claim runtime compatibility until tested in an actual Plasma 5.27 session.
- Use only Plasma 5.27 APIs verified against KDE official source or documentation.

---

### Task 1: Define the Plasma 5 compatibility contract

**Files:**
- Create: `tests/test_plasma5_contract.py`

**Interfaces:**
- Consumes: QML files, metadata, installer, and README.
- Produces: static regression checks for the branch compatibility boundary.

- [ ] Write tests requiring versioned Qt/KDE imports, `Item`/`Plasmoid.*`, `PlasmaCore.DataSource`, Plasma 5 metadata, `kpackagetool5`, and documented platform floors.
- [ ] Run the new test module and confirm failures against the Plasma 6 implementation.

### Task 2: Port runtime and configuration QML

**Files:**
- Modify: `package/contents/ui/main.qml`
- Modify: `package/contents/ui/CompactRepresentation.qml`
- Modify: `package/contents/ui/FullRepresentation.qml`
- Modify: `package/contents/ui/ProviderIcon.qml`
- Modify: `package/contents/ui/config/ConfigGeneral.qml`
- Modify: `package/contents/ui/config/ClaudeProfilesEditor.qml`
- Modify: `package/contents/config/config.qml`

**Interfaces:**
- Consumes: unchanged helper command and normalized report schema.
- Produces: Qt 5.15/KF5-compatible widget and settings QML.

- [ ] Replace Plasma 6-only roots/imports and data sources with verified Plasma 5.27 equivalents.
- [ ] Run the compatibility contract and existing QML contract tests until green.

### Task 3: Port packaging and document the branch

**Files:**
- Modify: `package/metadata.json`
- Modify: `install.sh`
- Modify: `README.md`

**Interfaces:**
- Consumes: Plasma 5 package layout and stable plugin ID.
- Produces: `kpackagetool5` installation and explicit Plasma 5.27/Python 3.10 documentation.

- [ ] Update metadata, installer commands, restart guidance, architecture description, and platform status.
- [ ] Run compatibility tests, JSON/XML parsing, `bash -n`, Python compilation, complete unit tests, and `git diff --check`.
- [ ] Record that real Plasma 5.27 runtime testing remains pending.
